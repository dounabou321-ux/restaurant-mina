# TODO - Order Delete Functionality

## Task: Fix order deletion for admin dashboard

### Steps:
1. [x] Add `delete_order` view in `restaurant/views.py`
2. [x] Add URL route for `delete_order` in `restaurant/urls.py`
3. [x] Add the missing `deleteOrder` JavaScript function in `templates/dashboard/orders.html`
